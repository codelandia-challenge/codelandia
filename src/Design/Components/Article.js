import { styled } from "../../stitches.config";

const Article = styled("article", {
  minHeight: "500px",
  border: "1px solid $purple_transparent",
  borderRadius: "15px",
  padding: "$gap",
  backgroundColor: "$dark_200",
})
export default Article;
